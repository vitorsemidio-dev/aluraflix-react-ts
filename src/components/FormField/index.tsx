import React from 'react';

import { FormFieldWrapper, Label, LabelText, Input } from './styles';

const FormField: React.FC = () => {
  return (
    <div>
      <span>FormField Component</span>
    </div>
  );
};

export default FormField;
